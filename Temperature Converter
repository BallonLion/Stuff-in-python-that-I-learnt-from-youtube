unit = input("Is this unit in Celcius or Fahrenheit(C/F):")
temp = float(input("Enter the temperature:"))

if unit == "C":
    temp = (9*temp)/5+32
    print(f"The temperature in fahrenheit is {temp}")
elif unit == "F":
    temp = (temp - 32)*5/9
else:
    print(f"{unit} is invalid")
